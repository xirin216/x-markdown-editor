This directory is populated by `npm run sync:vditor`.

`public/vditor/dist` is copied from `node_modules/vditor/dist` before development and production builds so the packaged Tauri app can load Vditor's static assets.
